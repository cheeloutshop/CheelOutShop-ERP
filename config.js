/* =========================================================
   CONFIGURAÇÃO DO ERP — edite este arquivo no GitHub
   ========================================================= */
window.ERP_CONFIG = {
  // Cole aqui a URL do App da Web do Apps Script (termina em /exec).
  // Deixe vazio ('') para usar o modo local (dados só no navegador).
  apiUrl: '',

  // E-mail de acesso ao sistema
  email: 'cheeloutshop@gmail.com',
};
